{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Allow the bundled main window to appear after its first frame and start native dragging.",
  "windows": ["main"],
  "permissions": [
    "core:window:allow-show",
    "core:window:allow-start-dragging"
  ]
}

